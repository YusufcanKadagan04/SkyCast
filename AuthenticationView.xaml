<Window x:Class="SkyCast.AuthenticationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Giriş Yap" Height="500" Width="400"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">

    <Border CornerRadius="20" Background="#202b3b" BorderBrush="#3a4b61" BorderThickness="1">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Click="BtnClose_Click" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Cursor="Hand">
                <TextBlock Text="✕" Foreground="Gray" FontSize="16"/>
            </Button>

            <StackPanel Grid.Row="1" Margin="0,10,0,30">
                <TextBlock x:Name="txtTitle" Text="Welcome Back" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock x:Name="txtSubtitle" Text="Please sign in to continue" FontSize="14" Foreground="#A0A0A0" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel x:Name="pnlLogin" Grid.Row="2" Visibility="Visible">
                <TextBlock Text="Username" Foreground="#A0A0A0" Margin="0,0,0,5"/>
                <Border Background="#151e2b" CornerRadius="10" Height="40" Margin="0,0,0,15">
                    <TextBox x:Name="txtLoginUser" Foreground="White" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" Margin="10,0"/>
                </Border>

                <TextBlock Text="Password" Foreground="#A0A0A0" Margin="0,0,0,5"/>
                <Border Background="#151e2b" CornerRadius="10" Height="40" Margin="0,0,0,25">
                    <PasswordBox x:Name="txtLoginPass" Foreground="White" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" Margin="10,0"/>
                </Border>

                <Button Click="BtnLogin_Click" Height="45" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#0099FF" CornerRadius="10">
                                <TextBlock Text="Sign In" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <TextBlock Text="Don't have an account?" Foreground="Gray"/>
                    <TextBlock Text=" Sign Up" Foreground="#0099FF" FontWeight="Bold" Cursor="Hand" MouseLeftButtonDown="BtnSwitchToRegister_Click"/>
                </StackPanel>
            </StackPanel>

            <StackPanel x:Name="pnlRegister" Grid.Row="2" Visibility="Collapsed">
                <TextBlock Text="Username" Foreground="#A0A0A0" Margin="0,0,0,5"/>
                <Border Background="#151e2b" CornerRadius="10" Height="40" Margin="0,0,0,15">
                    <TextBox x:Name="txtRegUser" Foreground="White" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" Margin="10,0"/>
                </Border>

                <TextBlock Text="Password" Foreground="#A0A0A0" Margin="0,0,0,5"/>
                <Border Background="#151e2b" CornerRadius="10" Height="40" Margin="0,0,0,15">
                    <PasswordBox x:Name="txtRegPass" Foreground="White" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" Margin="10,0"/>
                </Border>

                <TextBlock Text="Confirm Password" Foreground="#A0A0A0" Margin="0,0,0,5"/>
                <Border Background="#151e2b" CornerRadius="10" Height="40" Margin="0,0,0,25">
                    <PasswordBox x:Name="txtRegPassConfirm" Foreground="White" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" Margin="10,0"/>
                </Border>

                <Button Click="BtnRegister_Click" Height="45" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#00CA4E" CornerRadius="10">
                                <TextBlock Text="Create Account" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <TextBlock Text="Already have an account?" Foreground="Gray"/>
                    <TextBlock Text=" Sign In" Foreground="#0099FF" FontWeight="Bold" Cursor="Hand" MouseLeftButtonDown="BtnSwitchToLogin_Click"/>
                </StackPanel>
            </StackPanel>

        </Grid>
    </Border>
</Window>